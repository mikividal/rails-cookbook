<h1>My Categories</h1>

<% @categories.each do |category| %>
  <h2><%= link_to category.name, category_path(category) %> </h2>
<% end %>

<%= link_to "Create new category", new_category_path %> </h2>
